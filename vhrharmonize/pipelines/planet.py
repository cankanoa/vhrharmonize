"""Planet pipeline scaffold."""

from typing import Dict


def run_planet_pipeline(config: Dict) -> int:
    """Run Planet preprocessing pipeline.

    TODO: wire provider-specific discovery, metadata parsing, and processing stages.
    """
    _ = config
    raise NotImplementedError("Planet pipeline scaffold added; implementation pending.")
